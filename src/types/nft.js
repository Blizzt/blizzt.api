/**
 * NFTs have different types to classify their species:
 * OBJECT: Represents an element that is integrated into a game, it is usable.
 * CHROME: Represents a reward item, is not usable, but contains value.
 */
export const nftTypes = {
  OBJECT: 'object',
  CHROME: 'chrome',
};
